import { Request, Response, NextFunction } from 'express';
import { OpenpanelSdk, OpenpanelSdkOptions } from '@openpanel/sdk';
export * from '@openpanel/sdk';

declare global {
    namespace Express {
        interface Request {
            op: OpenpanelSdk;
        }
    }
}
type OpenpanelOptions = OpenpanelSdkOptions & {
    trackRequest?: (url: string) => boolean;
    getProfileId?: (req: Request) => string;
};
declare function createMiddleware(options: OpenpanelOptions): (req: Request, res: Response, next: NextFunction) => void;

export { type OpenpanelOptions, createMiddleware as default };
